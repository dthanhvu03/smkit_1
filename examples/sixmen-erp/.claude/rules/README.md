# Claude Code rules — parity với Cursor `alwaysApply`

> **Canonical (sửa trước):** `.cursor/rules/*.mdc`  
> **Mirror Claude (rút gọn):** file này + `sixmen-always-on-parity.md`  
> **Đồng bộ:** khi đổi guard quan trọng trong `.mdc` → cập nhật section tương ứng trong `sixmen-always-on-parity.md` + `rules-changelog.md`

`CLAUDE.md` import `@.claude/rules/sixmen-always-on-parity.md` để Claude Code có cùng guardrail cốt lõi như Cursor inject 6 rules.

Chi tiết Human: `docs/ai-agent/reference/platform-agent-parity.md`
