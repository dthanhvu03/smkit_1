# Generated agent config when experimenting (outDir: dist).
# When a real project sets outDir: ".", remove this and commit the generated files.
dist/

node_modules/

# Per-developer, not shared
.kit/*.local.md

# Guard audit log (runtime, not tamper-evident)
.kit/audit.log

# Ownership manifest (per-project build state, regenerated each build)
.kit/build-manifest.json


#Ignore cursor AI rules
.cursor\rules\codacy.mdc
